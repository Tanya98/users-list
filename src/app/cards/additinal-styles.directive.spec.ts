import { AdditinalStylesDirective } from './additinal-styles.directive';

describe('AdditinalStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new AdditinalStylesDirective();
    expect(directive).toBeTruthy();
  });
});
