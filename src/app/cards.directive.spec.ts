import { CardsDirective } from './cards.directive';

describe('CardsDirective', () => {
  it('should create an instance', () => {
    const directive = new CardsDirective();
    expect(directive).toBeTruthy();
  });
});
